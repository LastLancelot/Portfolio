export {};

const message: string = 'Hello, TypeScript!';
const showMessage = (name: string): void => {
    console.log(`There is message: ${name}`)
}

console.log(message);

const name: string = "Taras";